/** @type {import('next').NextConfig} */
const nextConfig = {
  // experimental: {
  //   after: true,
  //   ppr: 'incremental',
  //   reactCompiler: {
  //     compilationMode: 'annotation'
  //   }
  // }
};

export default nextConfig;
